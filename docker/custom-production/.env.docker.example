# ============================================================================
# Royal Dental Services - Docker Environment Configuration
# 
# Copy this file to .env.docker and customize for your deployment
# ============================================================================

# Application Settings
APP_URL=http://localhost:8080
APP_PORT=8080
SESSION_DOMAIN=localhost
SANCTUM_STATEFUL_DOMAINS=localhost:8080

# Database Passwords (CHANGE THESE IN PRODUCTION!)
DB_PASSWORD=InvoiceShelf_2025_Secure!
DB_ROOT_PASSWORD=RootPass_2025!

# Mail Configuration (Optional - configure if you want to send emails)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM_ADDRESS=noreply@royaldentalservices.com

# Timezone
TZ=Africa/Nairobi

# ============================================================================
# PRODUCTION DEPLOYMENT CHECKLIST:
# 
# 1. Change DB_PASSWORD and DB_ROOT_PASSWORD to strong random passwords
# 2. Update APP_URL to your actual domain (e.g., https://invoices.yourdomain.com)
# 3. Update SESSION_DOMAIN and SANCTUM_STATEFUL_DOMAINS to match your domain
# 4. Configure mail settings if you want email functionality
# 5. Use HTTPS in production (recommended: nginx-proxy with Let's Encrypt)
# 6. Set up regular backups of the invoiceshelf_mysql_data volume
# 7. Monitor logs: docker-compose logs -f webapp
# ============================================================================
